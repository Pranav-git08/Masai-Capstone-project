# Data Pipeline Module

This module automates scraping book dataset records, cleans and validates raw fields, ingests structured data into a normalized SQLite database, executes analytical queries, and validates output equivalency using Pandas.

## Setup & Execution

### Prerequisites
Ensure Python 3.12+ and required libraries are installed:

```bash
pip install pandas requests beautifulsoup4 
